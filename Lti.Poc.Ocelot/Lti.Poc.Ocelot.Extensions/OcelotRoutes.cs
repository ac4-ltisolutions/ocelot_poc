using System.Collections.Generic;

namespace Lti.Poc.Ocelot.Extensions
{
    public static class OcelotRoutes
    {
        public static void AddLtiOcolotRouts(IEnumerable<string> routeNames)
        {
            foreach(var name in routeNames)
            {
                // TODO: add routes to ocelot
            }
        }
    }
}
